# Regression fixtures

One fixture per historical escape — a defect a PASS once missed. Each fixture must fail on the
old verification and pass on the patched one, so the same gap can never pass silently twice.
(Populated by /capture-gap in Phase 4.)
